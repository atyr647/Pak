# Programs whose generated C does NOT compile against the MODULE_API stubs.
#
# The corpus is tcl/tools/gate_corpus.tcl: examples/canonical plus every
# single-file demo.
#
# examples/dungeon_of_types/src/main.pk64 is written against constructs Pak
# does not have and LANGUAGE.md does not document: bare variant constructors
# (`return hit(dmg, hp)` rather than `DamageResult.hit(...)`, section 6) and
# associated functions with no self (`Player.init()` called on the type,
# where impl blocks take `self`, section 6). `pak check` accepts it because
# neither is diagnosed yet, which is its own bug. Either the demo is rewritten
# in documented Pak or the constructs are implemented; until then this is the
# record that it does not build.
#
# Maintained by tcl/tools/c_compile_test.tcl. This is debt, not
# configuration: fix a backend bug, drop the name, and the gate holds
# the new floor. Adding a name is how a regression gets waved through,
# so add one only with the bug it records written down.

examples/dungeon_of_types/src/main.pk64
