variant Opt {
    some(i32)
    none
}
fn check(v: Opt) -> i32 {
    match v {
        .some(x) if x > 0 => { return x }
        .some(x) => { return 0 }
        .none => { return -1 }
    }
}
entry {}
