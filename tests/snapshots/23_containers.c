/* Generated by Pak Compiler - examples/canonical/23_containers.pk64 */

#include <libdragon.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>
#include "pak_math.h"
#include "pak_containers.h"
#include "pak_libdragon.h"


/* -- Pak runtime types -- */
typedef struct { const char *data; int32_t len; } PakStr;
typedef struct { uint8_t *base; uint8_t *ptr; size_t capacity; } PakArena;
static inline PakStr pak_str_from_cstr(const char *s) {
    return (PakStr){ .data = s, .len = (int32_t)strlen(s) }; }
static inline bool pak_str_eq(PakStr a, PakStr b) {
    return a.len == b.len && memcmp(a.data, b.data, (size_t)a.len) == 0; }
static inline void *pak_arena_alloc(PakArena *a, size_t sz) {
    sz = (sz + 7) & ~(size_t)7;  /* 8-byte align */
    if (a->ptr + sz > a->base + a->capacity) return NULL;
    void *p = a->ptr; a->ptr += sz; return p; }
static inline void pak_arena_reset(PakArena *a) { a->ptr = a->base; }

/* -- User type forward declarations -- */
typedef struct Bullet Bullet;

/* -- User types -- */
struct Bullet {
    float x;
    float y;
    int32_t active;
};


/* -- Vec(T) dynamic vector -- */
#include <stdlib.h>
#define _PAK_VEC_PUSH(v, item) do { \
    if ((v)->len >= (v)->cap) { \
        (v)->cap = (v)->cap ? (v)->cap * 2 : 8; \
        (v)->data = realloc((v)->data, (size_t)(v)->cap * sizeof(*(v)->data)); \
    } \
    (v)->data[(v)->len++] = (item); \
} while(0)
typedef struct {
    int32_t *data;
    int32_t len;
    int32_t cap;
} _PakVec_int32_t;

/* -- Container types -- */
typedef struct {
    Bullet data[32];
    int32_t len;
} _PakList_Bullet_32;

typedef struct {
    float data[8];
    int32_t head, tail, len;
} _PakRBuf_float_8;

typedef struct {
    int32_t keys[16];
    int32_t values[16];
    bool occupied[16];
    int32_t len;
} _PakMap_int32_t_int32_t_16;

typedef struct {
    Bullet data[16];
    int32_t len;
} _PakPool_Bullet_16;

enum { MAX_BULLETS = 32 };

enum { MAX_RING = 8 };

enum { MAP_CAP = 16 };

static int32_t sink_i = 0;

static float sink_f = 0.0f;

int main(void) {
    _PakList_Bullet_32 bullets = {0};
    ((bullets).len < 32 ? ((bullets).data[(bullets).len++] = ((Bullet){.x = 10.0f, .y = 20.0f, .active = 1}), 1) : 0);
    ((bullets).len < 32 ? ((bullets).data[(bullets).len++] = ((Bullet){.x = 5.0f, .y = 15.0f, .active = 1}), 1) : 0);
    sink_i = (bullets).len;
    __auto_type chk = ((bullets).len == 0);
    __auto_type b = (bullets).data[--(bullets).len];
    sink_f = b.x;
    sink_i = (bullets).len;
    ({ int32_t _ri = (0); (bullets).data[_ri] = (bullets).data[--(bullets).len]; });
    sink_i = (bullets).len;
    _PakRBuf_float_8 ring = {0};
    ({ (ring).data[(ring).tail] = (1.0f); (ring).tail = ((ring).tail + 1) % 8; if ((ring).len < 8) (ring).len++; });
    ({ (ring).data[(ring).tail] = (2.0f); (ring).tail = ((ring).tail + 1) % 8; if ((ring).len < 8) (ring).len++; });
    ({ (ring).data[(ring).tail] = (3.0f); (ring).tail = ((ring).tail + 1) % 8; if ((ring).len < 8) (ring).len++; });
    sink_i = (ring).len;
    __auto_type last = (ring).data[((ring).tail - (0) - 1 + 8) % 8];
    sink_f = last;
    __auto_type oldest = ({ __auto_type _v = (ring).data[(ring).head]; (ring).head = ((ring).head + 1) % 8; if ((ring).len > 0) (ring).len--; _v; });
    sink_f = oldest;
    sink_i = (int32_t)((ring).len == 0);
    _PakMap_int32_t_int32_t_16 scores = {0};
    pak_map_set(&(scores), 16, 1, 100);
    pak_map_set(&(scores), 16, 2, 200);
    pak_map_set(&(scores), 16, 3, 300);
    sink_i = (scores).len;
    __auto_type ptr = ((int32_t *)pak_map_get(&(scores), 16, 2));
    if (ptr != NULL) {
        sink_i = *ptr;
    }
    __auto_type has_key = pak_map_has(&(scores), 16, 3);
    pak_map_remove(&(scores), 16, 1);
    sink_i = (scores).len;
    _PakPool_Bullet_16 pool = {0};
    Bullet * slot = ((Bullet *)pak_pool_acquire(&(pool)));
    if (slot != NULL) {
        slot->x = 7.0f;
        slot->active = 1;
        sink_f = slot->x;
        pak_pool_release(&(pool), slot);
    }
    sink_i = (pool).len;
    _PakVec_int32_t nums = {0};
    _PAK_VEC_PUSH(&(nums), (10));
    _PAK_VEC_PUSH(&(nums), (20));
    _PAK_VEC_PUSH(&(nums), (30));
    sink_i = (nums).len;
    __auto_type first = (nums).data[0];
    sink_i = first;
    __auto_type empty_v = ((nums).len == 0);
    ((nums).len = 0);
    sink_i = (nums).len;
    _PAK_VEC_PUSH(&(nums), (99));
    ((nums).cap < (64) ? ((nums).data = (int32_t *)realloc((nums).data, (size_t)(64) * sizeof(*(nums).data)), (nums).cap = (64), (void)0) : (void)0);
    sink_i = (nums).len;
    ({ free((nums).data); (nums).data = NULL; (nums).len = (nums).cap = 0; });
    return 0;
}

