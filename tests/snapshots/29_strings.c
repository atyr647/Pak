/* Generated by Pak Compiler - examples/canonical/29_strings.pk64 */

#include <libdragon.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>
#include "pak_math.h"
#include "pak_containers.h"


/* -- Pak runtime types -- */
typedef struct { const char *data; int32_t len; } PakStr;
typedef struct { uint8_t *base; uint8_t *ptr; size_t capacity; } PakArena;
static inline PakStr pak_str_from_cstr(const char *s) {
    return (PakStr){ .data = s, .len = (int32_t)strlen(s) }; }
static inline bool pak_str_eq(PakStr a, PakStr b) {
    return a.len == b.len && memcmp(a.data, b.data, (size_t)a.len) == 0; }
static inline void *pak_arena_alloc(PakArena *a, size_t sz) {
    sz = (sz + 7) & ~(size_t)7;  /* 8-byte align */
    if (a->ptr + sz > a->base + a->capacity) return NULL;
    void *p = a->ptr; a->ptr += sz; return p; }
static inline void pak_arena_reset(PakArena *a) { a->ptr = a->base; }
bool check_greeting(const char * s) {
    return ((strncmp(s, "hello", strlen("hello")) == 0) && (strstr(s, "world") != NULL));
}

bool same_string(const char * a, const char * b) {
    return (strcmp(a, b) == 0);
}

int32_t find_offset(const char * haystack, const char * needle) {
    return ({ const char *_h = strstr(haystack, needle); _h ? (int32_t)(_h - (haystack)) : -1; });
}

int32_t check_pakstr(PakStr s) {
    return (s).len;
}

static int32_t sink_i = 0;

static bool sink_b = false;

int main(void) {
    const char * s = "hello world";
    sink_b = (strstr(s, "world") != NULL);
    sink_b = (strncmp(s, "hello", strlen("hello")) == 0);
    sink_b = (strlen(s) >= strlen("world") && strcmp((s) + strlen(s) - strlen("world"), "world") == 0);
    sink_b = (strcmp(s, "hello world") == 0);
    sink_b = (strcmp(s, "goodbye") == 0);
    sink_i = ({ const char *_h = strstr(s, "world"); _h ? (int32_t)(_h - (s)) : -1; });
    sink_i = ({ const char *_h = strstr(s, "hello"); _h ? (int32_t)(_h - (s)) : -1; });
    sink_i = ({ const char *_h = strstr(s, "xyz"); _h ? (int32_t)(_h - (s)) : -1; });
    sink_b = ((s)[0] == '\0');
    const char * empty = "";
    sink_b = ((empty)[0] == '\0');
    sink_i = (s).len;
    sink_b = check_greeting(s);
    sink_b = same_string(s, "hello world");
    sink_b = same_string(s, "other");
    sink_i = find_offset(s, "world");
    PakStr ps = pak_str_from_cstr("pak string");
    sink_i = check_pakstr(ps);
    sink_b = ((ps).len == 0);
    PakStr ps2 = pak_str_from_cstr(s);
    sink_i = (ps2).len;
    const char * tail = ((s) + (6));
    sink_b = (strcmp(tail, "world") == 0);
    sink_b = (strncmp(tail, "world", strlen("world")) == 0);
    return 0;
}

