/* Generated by Pak Compiler - examples/canonical/06_variants.pk64 */

#include <libdragon.h>
#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>
#include "pak_math.h"
#include "pak_containers.h"
#include "pak_libdragon.h"


/* -- Pak runtime types -- */
typedef struct { const char *data; int32_t len; } PakStr;
typedef struct { uint8_t *base; uint8_t *ptr; size_t capacity; } PakArena;
static inline PakStr pak_str_from_cstr(const char *s) {
    return (PakStr){ .data = s, .len = (int32_t)strlen(s) }; }
static inline bool pak_str_eq(PakStr a, PakStr b) {
    return a.len == b.len && memcmp(a.data, b.data, (size_t)a.len) == 0; }
static inline void *pak_arena_alloc(PakArena *a, size_t sz) {
    sz = (sz + 7) & ~(size_t)7;  /* 8-byte align */
    if (a->ptr + sz > a->base + a->capacity) return NULL;
    void *p = a->ptr; a->ptr += sz; return p; }
static inline void pak_arena_reset(PakArena *a) { a->ptr = a->base; }

/* -- User types -- */
typedef enum {
    Entity_tag_player,
    Entity_tag_enemy,
    Entity_tag_projectile,
    Entity_tag_empty,
} Entity_tag;

typedef struct {
    Entity_tag tag;
    union {
    } data;
} Entity;

typedef struct {
    float field0;
} Shape_circle;

typedef struct {
    float field0;
    float field1;
} Shape_rect;

typedef enum {
    Shape_tag_circle,
    Shape_tag_rect,
    Shape_tag_point,
} Shape_tag;

typedef struct {
    Shape_tag tag;
    union {
        Shape_circle circle;
        Shape_rect rect;
    } data;
} Shape;

typedef struct {
    uint8_t field0;
} Event_key_press;

typedef struct {
    int32_t field0;
    int32_t field1;
} Event_mouse_move;

typedef enum {
    Event_tag_key_press,
    Event_tag_mouse_move,
    Event_tag_quit,
} Event_tag;

typedef struct {
    Event_tag tag;
    union {
        Event_key_press key_press;
        Event_mouse_move mouse_move;
    } data;
} Event;

typedef struct {
    int32_t id;
    uint8_t port;
} Packet_connect;

typedef struct {
    int32_t len;
    uint32_t checksum;
} Packet_data;

typedef enum {
    Packet_tag_connect,
    Packet_tag_data,
    Packet_tag_disconnect,
} Packet_tag;

typedef struct {
    Packet_tag tag;
    union {
        Packet_connect connect;
        Packet_data data;
    } data;
} Packet;


/* -- Function prototypes -- */
float area(Shape s);
int32_t entity_id(Entity e);
float area(Shape s) {
    switch (s.tag) {
        case Shape_tag_circle:
        {
            __auto_type r = s.data.circle.field0;
            return ((r * r) * 3.14159f);
            break;
        }
        case Shape_tag_rect:
        {
            __auto_type w = s.data.rect.field0;
            __auto_type h = s.data.rect.field1;
            return (w * h);
            break;
        }
        case Shape_tag_point:
        {
            return 0.0f;
            break;
        }
        default: __builtin_unreachable();
    }
}

int32_t entity_id(Entity e) {
    switch (e.tag) {
        case Entity_tag_player:
        {
            return 0;
            break;
        }
        case Entity_tag_enemy:
        {
            return 1;
            break;
        }
        case Entity_tag_projectile:
        {
            return 2;
            break;
        }
        case Entity_tag_empty:
        {
            return 3;
            break;
        }
        default: __builtin_unreachable();
    }
}

static float sink = 0.0f;

static int32_t sink_i = 0;

int main(void) {
    sink = area((Shape){ .tag = Shape_tag_circle, .data.circle = { .field0 = 5.0f } });
    sink = area((Shape){ .tag = Shape_tag_rect, .data.rect = { .field0 = 4.0f, .field1 = 3.0f } });
    sink = area((Shape){ .tag = Shape_tag_point });
    sink_i = entity_id((Entity){ .tag = Entity_tag_enemy });
    __auto_type e = (Event){ .tag = Event_tag_mouse_move, .data.mouse_move = { .field0 = 100, .field1 = 200 } };
    switch (e.tag) {
        case Event_tag_key_press:
        {
            __auto_type k = e.data.key_press.field0;
            sink_i = (int32_t)k;
            break;
        }
        case Event_tag_mouse_move:
        {
            __auto_type x = e.data.mouse_move.field0;
            __auto_type y = e.data.mouse_move.field1;
            sink_i = (x + y);
            break;
        }
        case Event_tag_quit:
        {
            sink_i = -1;
            break;
        }
        default: __builtin_unreachable();
    }
    __auto_type pkt = (Packet){.tag = Packet_tag_connect, .data.connect = {.id = 42, .port = 80}};
    switch (pkt.tag) {
        case Packet_tag_connect:
        {
            sink_i = 1;
            break;
        }
        case Packet_tag_data:
        {
            sink_i = 2;
            break;
        }
        case Packet_tag_disconnect:
        {
            sink_i = 0;
            break;
        }
        default: __builtin_unreachable();
    }
    return 0;
}

