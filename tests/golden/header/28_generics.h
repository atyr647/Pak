/* Generated by Pak Compiler - demo.28_generics */
#ifndef PAKMOD_DEMO_28_GENERICS_H
#define PAKMOD_DEMO_28_GENERICS_H

#include <stdint.h>
#include <stdbool.h>

typedef struct {
    T first;
    T second;
} Pair;

typedef struct {
    K key;
    V value;
} KeyVal;

T identity(T x);
T max_of(T a, T b);
T swap_first(T a, T b);

#endif /* PAKMOD_DEMO_28_GENERICS_H */