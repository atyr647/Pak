/* Generated by Pak Compiler - demo.30_raycaster */
#ifndef PAKMOD_DEMO_30_RAYCASTER_H
#define PAKMOD_DEMO_30_RAYCASTER_H

#include <stdint.h>
#include <stdbool.h>

typedef enum {
    GamePhase_title,
    GamePhase_playing,
    GamePhase_paused,
    GamePhase_gameover,
} GamePhase;

typedef struct {
    float pos_x;
    float pos_y;
    float dir_x;
    float dir_y;
    float plane_x;
    float plane_y;
} Camera;

typedef struct {
    Camera cam;
    GamePhase phase;
    int32_t frame;
    int32_t health;
    int32_t score;
} GameState;

uint8_t map_get(int32_t mx, int32_t my);
uint32_t wall_color(uint8_t wtype, int32_t side);
void render_minimap(GameState * gs);
void render_3d(GameState * gs);
void update_playing(GameState * gs, joypad_status_t pad);
void render_screen(GameState * gs);
void init_game(GameState * gs);

#endif /* PAKMOD_DEMO_30_RAYCASTER_H */
