(W005 warning "'rdpq.set_tile_mask' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
(W005 warning "'rdpq.set_tri_z' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
(W005 warning "'rdpq.triangle_tex_z' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
(W005 warning "'rdpq.set_tri_z' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
(W005 warning "'rdpq.triangle_tex_z' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
(W005 warning "'rdpq.set_mode_standard_z' is not implemented on the libdragon backend" "Pak names it but neither libdragon nor Tiny3D defines it, so the generated C will not compile. It exists on the standalone HAL: build with --backend mips. See STDLIB.md.")
